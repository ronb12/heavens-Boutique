{
  "version": 2,
  "framework": null,
  "installCommand": "cp backend/package.json backend/package-lock.json . && npm ci",
  "buildCommand": "rm -rf api lib public && cp -R backend/api api && cp -R backend/lib lib && mkdir -p public && cp backend/public/index.html public/",
  "crons": [
    {
      "path": "/api/cron/abandoned-cart",
      "schedule": "0 * * * *"
    }
  ]
}

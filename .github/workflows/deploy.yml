name: Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      apply_database:
        description: "Apply database/schema.sql to Neon (even if no DB files changed)"
        required: false
        type: choice
        default: "no"
        options:
          - "no"
          - "yes"

concurrency:
  group: deploy-heavens-boutique-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    env:
      NEON_PROJECT_ID: ${{ vars.NEON_PROJECT_ID }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Detect database / script changes
        uses: dorny/paths-filter@v3
        id: changes
        with:
          filters: |
            database:
              - 'database/**'
              - 'scripts/neon-apply-schema.sh'

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: backend/package-lock.json

      - name: Install PostgreSQL client
        run: sudo apt-get update && sudo apt-get install -y postgresql-client

      - name: Apply schema to Neon
        if: |
          (github.event_name == 'push' && steps.changes.outputs.database == 'true') ||
          (github.event_name == 'workflow_dispatch' && github.event.inputs.apply_database == 'yes')
        env:
          NEON_API_KEY: ${{ secrets.NEON_API_KEY }}
        run: |
          set -euo pipefail
          if [ -z "${NEON_API_KEY:-}" ]; then
            echo "::warning::NEON_API_KEY is not set; skipping Neon schema apply."
            exit 0
          fi
          PROJECT_ID="${NEON_PROJECT_ID:-withered-fog-14874911}"
          echo "Applying schema to Neon project $PROJECT_ID (branch: production)"
          CONN=$(npx neonctl@latest connection-string production \
            --project-id "$PROJECT_ID" \
            --pooled \
            --no-color)
          psql "$CONN" -v ON_ERROR_STOP=1 -f database/schema.sql
          echo "Neon schema step finished."

      # Match Vercel "Root Directory = ." (repo root). If the dashboard is set to `backend`
      # only, either switch Root Directory to "." here or deploy manually from backend/.
      - name: Prepare Vercel bundle at repo root
        run: |
          set -euo pipefail
          cp backend/package.json backend/package-lock.json .
          npm ci
          rm -rf api lib public
          cp -R backend/api api
          cp -R backend/lib lib
          mkdir -p public
          cp backend/public/index.html public/

      # Never commit .vercel/ — and do not echo-write project.json: current CLIs validate project
      # settings from the API; a minimal JSON triggers "Project Settings are invalid".
      - name: Deploy to Vercel (production)
        env:
          VERCEL_TELEMETRY_DISABLED: "1"
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
          # Split secret vs var — do not use ${{ secrets.x || vars.x || 'default' }}; GitHub can drop the literal fallback.
          VERCEL_ORG_SECRET: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_ORG_VAR: ${{ vars.VERCEL_ORG_ID }}
          VERCEL_PROJ_SECRET: ${{ secrets.VERCEL_PROJECT_ID }}
          VERCEL_PROJ_VAR: ${{ vars.VERCEL_PROJECT_ID }}
        run: |
          set -euo pipefail
          if [ -z "${VERCEL_TOKEN:-}" ]; then
            echo "::error::VERCEL_TOKEN is empty or not set. Add repository secret VERCEL_TOKEN (Vercel → Account Settings → Tokens)."
            exit 1
          fi
          # Bash fallback chain: secret → variable → defaults for this repo’s Vercel project
          DEFAULT_ORG='team_RyIheF5P8ysoBiLR1yU2UMQR'
          DEFAULT_PROJ='prj_rDcgpKuogIGsRDbqtzSAQqUivKfF'
          VERCEL_ORG_ID="${VERCEL_ORG_SECRET:-${VERCEL_ORG_VAR:-$DEFAULT_ORG}}"
          VERCEL_PROJECT_ID="${VERCEL_PROJ_SECRET:-${VERCEL_PROJ_VAR:-$DEFAULT_PROJ}}"
          export VERCEL_ORG_ID="$(echo -n "$VERCEL_ORG_ID" | tr -d '[:space:]')"
          export VERCEL_PROJECT_ID="$(echo -n "$VERCEL_PROJECT_ID" | tr -d '[:space:]')"
          rm -rf .vercel
          # Do not use --team/--scope with team_… IDs: newer CLI treats --scope as a slug and errors "scope does not exist".
          # Official CI pattern: VERCEL_ORG_ID + VERCEL_PROJECT_ID in the environment (see Vercel CLI global options).
          npx --yes vercel@50.37.3 deploy --prod --yes --token "$VERCEL_TOKEN"

      - name: Smoke test API (production)
        env:
          SMOKE_BASE: ${{ vars.VERCEL_SMOKE_BASE }}
        run: |
          set -euo pipefail
          base="${SMOKE_BASE:-https://heavens-boutique.vercel.app}"
          code=$(curl -sS -o /tmp/hb-api-smoke.json -w "%{http_code}" "${base}/api/products")
          if [ "$code" != "200" ]; then
            echo "Expected GET ${base}/api/products → 200, got HTTP $code"
            cat /tmp/hb-api-smoke.json || true
            exit 1
          fi
          echo "GET ${base}/api/products OK (200)"
